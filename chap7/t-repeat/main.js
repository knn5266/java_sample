// const arr = ['dog', 'cat', 'mouse'];
// const arr2 = arr
//   .filter((x) => {
//     return x.length > 0;
//   })
//   .map((x, i) => {
//     `${i}:${x}`;
//   });
// console.log(arr2.join(','));

// for (let i = 0; i < arr.length; i++) {
//   console.log(`${i}:${arr[i]}`);
// }

//answer
const arr = ['dog', 'cat', 'mouse'];
array.forEach((x, i) => {
  console.log(`${i}:${arr[i]}`);
});
